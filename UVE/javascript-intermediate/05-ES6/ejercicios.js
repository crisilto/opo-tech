// ===============================
// EJERCICIOS: MÓDULOS ES6
// ===============================

// 🧩 1. En `math.js`, exporta 3 funciones con nombre:
//    - sumar(a, b)
//    - restar(a, b)
//    - multiplicar(a, b)

// Luego, desde `main.js`, importa las tres y pruébalas con distintos números.

// 🧩 2. En `saludo.js`, exporta una función por defecto que reciba un nombre y diga "¡Hola, X!".
//    - Importa esa función por defecto en `main.js` y pruébala con tu nombre.

// 🧩 3. En `usuario.js`, exporta una clase `Usuario` con propiedades `nombre` y `edad`, y un método `presentarse()`.
//    - Importa esta clase desde `main.js`, crea un objeto y llama al método.

// 🧩 4. En `math.js`, además de las funciones anteriores, crea una constante `PI = 3.14` y expórtala.
//    - Importa solo esa constante en `main.js` usando desestructuración.

// 🧩 5. Usa `import * as` para importar todo lo que venga desde `math.js` bajo el nombre `calculadora`.
//    - Luego, llama a `calculadora.sumar()` y `calculadora.PI`.

// 🧩 6. BONUS: Crea una carpeta `utils/` y mueve `math.js` dentro.
//    - Ajusta las rutas de importación en `main.js` para que siga funcionando.
//    - Así practicas la organización modular del código.

// 🧩 7. BONUS 2: Inicializa un proyecto con Vite (`npm create vite@latest`) y crea los mismos archivos allí.
//    - Ejecuta `npm run dev` y verifica que las importaciones funcionen.
//    - Asegúrate de que los archivos `.js` estén en `src/`.

